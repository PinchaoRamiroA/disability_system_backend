version: '3.9'

services:
  postgres:
    image: postgres:latest
    container_name: disability_postgres
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password
      POSTGRES_DB: disability_system_db
    ports:
      - "5432:5432"
